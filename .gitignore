# Dependencies
node_modules/

# Build output — generated. NOT committed; CI (.github/workflows/pages.yml) builds & deploys
# it to GitHub Pages on every push to main. Regenerate locally with `npm run build`.
dist/

# Tooling / OS cruft
*.tsbuildinfo
*.log
npm-debug.log*
.DS_Store
