{
  "name": "comacpro-email-templates",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Marketing/transactional email templates for COMACPRO. Data-driven MJML compiled to bulletproof inline HTML.",
  "scripts": {
    "build": "tsx build/main.ts",
    "build:min": "tsx build/main.ts --minify",
    "watch": "tsx watch build/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "lint:email": "tsx build/lint-compat.ts",
    "validate": "tsc --noEmit && tsx build/main.ts && tsx build/lint-compat.ts && tsx --test test/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/nunjucks": "^3.2.6",
    "mjml": "^5.4.0",
    "nunjucks": "^3.2.4",
    "tsx": "^4.23.9",
    "typescript": "^7.0.2",
    "zod": "^4.4.3"
  }
}
