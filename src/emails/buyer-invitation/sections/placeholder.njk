{# SCAFFOLD placeholder — replace with real sections (hero, body, CTA…) from the design. #}
<mj-section padding="40px 32px 24px 32px">
  <mj-column>
    <mj-text color="{$ tokens.color.heading $}" font-size="18px" font-weight="700" padding="0 0 12px 0">
      Dear {{buyer_name}},
    </mj-text>
    <mj-text color="{$ tokens.color.muted $}" font-size="15px" line-height="1.6" padding="0">
      🚧 {$ content.placeholder.note $}
    </mj-text>
  </mj-column>
</mj-section>
