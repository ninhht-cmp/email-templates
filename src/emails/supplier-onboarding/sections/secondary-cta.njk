{% from "design-system/components/button.njk" import button %}
{# Secondary CTA + divider before the signature. #}
<mj-section padding="16px 40px 4px 40px">
  <mj-column>
    <mj-text align="center" color="{$ tokens.color.body $}" padding="0 0 18px 0">
      Let's explore how <span class="brand">COMACPRO</span> can help your company reach new B2B
      opportunities in Southeast Asia.
    </mj-text>
    {$ button('BECOME AN OFFICIAL SUPPLIER', '{{become_supplier_url}}', tokens.color.navy) $}
  </mj-column>
</mj-section>
<mj-section padding="24px 32px 0 32px">
  <mj-column><mj-divider border-width="1px" border-color="{$ tokens.color.divider $}" padding="0" /></mj-column>
</mj-section>
